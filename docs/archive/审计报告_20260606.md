# 🔧 烛照九阴 · 大修完成报告

> 日期：2026-06-06 · 执行：CC
> 背景：数灵转移（收官 2026-06-04）后，本项目沦为"迁移残壳"——数据已迁 `Database/`，但代码仍指阿里云死路径、文档停在 5-11。
> 决策：天哥定「脚本留项目、brain 管记忆、数据在 Database」，**大修 repoint 续用**（非归档）。

---

## 一、做了什么（5 步）

**1. 建路径单一可信源 `config.py`**（新）
项目根新增 `config.py`，解析公共数据层路径并自检：`RECAP_DB / NEWS_DB / MARKET_DB / RAW_RECAP_DIR / YUANTU_ROOT / ENV_FILE`。所有脚本经此取库，禁止再写死路径。`python3 config.py` 一键自检（实测全 ✅）。

**2. repoint 16 个现役脚本**
全部改为 `import config` 取库路径；修复 `from .lib.logger`/`from ..lib.logger` 相对导入（这是另一处隐患——旧代码作为脚本直接运行必报 ImportError，整条工具链本就跑不起来）；删除所有阿里云 `sys.path.insert("/home/admin/...")`。

**3. 归档 37 个一次性/退役脚本 → `_legacy/`**
旧建库/迁移脚本、课件批量提取、squeeze 历史榨取、财新/冈底斯弃用桥（D7）、v1 桥、龙宫梦境（D4）。详见 `_legacy/MANIFEST.md`。

**4. 刷新文档至 6-06 实况**
README / STATUS / INDEX / 外部资源对接 / news·STATUS 全部重写：真实表数（recap_daily 152、dim3 153、industry_signals 1231 等）、数据范围延到 2026-06-03、对齐 D5–D9（单一可信源、渊图主从倒置、小鲍 P2、jumang 作废、龙宫退役）。GOTCHAS 补 G011/G012。

**5. 加 `.gitignore` + 清理**
屏蔽 `__pycache__`/`*.db`/`*.env`/`.fuse_hidden`。（注：云同步 FUSE 挂载禁止删除旧 `.pyc`，已由 .gitignore 接管，无害。）

---

## 二、验证结果（实测）

| 验证项 | 结果 |
|--------|------|
| `config.py` 自检 6 路径 | ✅ 全部命中真库 |
| 全 live `.py` 编译 | ✅ 通过 |
| live 模块运行时 import + 连真库 | ✅ 15/16（recap_db 读到 152 条、范围 2026-06-03）|
| `recap_cli.py stats / emotion / dates / by-date` | ✅ 正常输出真数据 |
| 残留阿里死路径（live，排除 _legacy） | ✅ 无 |
| 残留相对 logger 导入（live） | ✅ 无 |
| 16 脚本均 `import config` | ✅ 是 |

---

## 三、遗留（非阻断）

| 项 | 严重度 | 说明 |
|----|--------|------|
| `cycle_compare.py` 报错 | 🟡 | **G011**：依赖 `cycle_quant.calculate_emotion_score`，该函数名已变（历史 API 漂移，非本次 repoint 引入）。需对齐 cycle_quant 现公开函数名。这是唯一未跑通的 live 模块。 |
| `news_events` 表空 | 🟢 | 新闻事件聚类层尚未落数据（news_raw/cleaned 各 17）。 |
| `__pycache__` 旧 .pyc 残留 | 🟢 | FUSE 挂载禁删，.gitignore 已屏蔽。 |
| emotion_stage 29 条空 | 🟢 | 5 月后新录入未标情绪阶段。 |
| `tushare_pipeline.py` 职责 | 🟢 | 已 repoint，但与句芒公共 `market-data-daily-update`（D8）可能重叠，建议确认归属再决定保留/退役。 |

---

## 四、新结构速览

```
烛照九阴/
├── config.py          ⭐ 路径单一可信源
├── tools/ (10)        现役工具，均 import config
├── scripts/ (4)       enhance_with_jumang / tushare_pipeline / stock_stats / exec_logger
├── news/              recap_bridge_v2 + news_cleaner（现役）
├── lib/ credentials/ db/(*.sql) data/ docs/(15) raw/4-dims(语料)
├── _legacy/ (37)      退役归档 + MANIFEST.md
└── .gitignore         ⭐ 新
数据：Database/烛照九阴/{recap.db,news.db} + Database/Market-Data/（只读）
记忆：brain/agents/烛阴/   产业逻辑：Database/行业研究/（渊图，只读）
```

---

## 五、建议下一步

1. 修 **G011**（cycle_compare）：让我把它和 cycle_quant 的公开函数对齐，10 分钟。
2. 在 brain 落一条 `/save` 记录这次大修（记忆系统留痕）。
3. 可选：给 recap_cli 加一个"文档数字 ⟷ 真库行数"自检命令，防再次失真。

---

*大修完成报告 · 2026-06-06 · CC*
